import 'package:flutter/material.dart';

class PictureUploadForm extends StatelessWidget {
  const PictureUploadForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
